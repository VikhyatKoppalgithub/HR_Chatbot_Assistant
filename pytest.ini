[pytest]
testpaths = tests

markers =
    slow: exercises a real model rather than a stub (skip with -m "not slow")

# Quiet third-party deprecation chatter from the ML stack so real failures
# stand out. Our own warnings are still shown.
filterwarnings =
    ignore::FutureWarning:sentence_transformers.*
    ignore::DeprecationWarning:transformers.*
